# Wave Dispersion Relation

The linear dispersion relation is given by:

$$
\omega^2 = g k \tanh(k h)
$$

where:
- \(g\) — gravitational acceleration,
- \(k\) — wave number,
- \(h\) — water depth.

Example inline citation: see {cite}`smith2020`.
